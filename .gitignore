# 虚拟环境
.venv/

# Python 缓存
__pycache__/
*.pyc

# 上传的照片（属于私密内容，不要提交到公开仓库）
uploads/

# 两个人的记录（运行时生成，属于私密内容，不要提交）
# 只放行三个静态内容库，其余 data/*.json 一律不上传
data/*.json
!data/quotes.json
!data/business_phrases.json
!data/poems.json
data/*.tmp

# 密钥（如果以后加了访问密码就放这里）
.streamlit/secrets.toml

# macOS
.DS_Store
